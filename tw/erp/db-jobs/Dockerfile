# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tw-erp-db-jobs`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tw-erp-db-jobs`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tw-erp-db-jobs && \
          adduser --system -G tw-erp-db-jobs tw-erp-db-jobs

COPY dist/tw/erp/db-jobs tw-erp-db-jobs
RUN chown -R tw-erp-db-jobs:tw-erp-db-jobs .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tw-erp-db-jobs --omit=dev -f install

CMD [ "node", "tw-erp-db-jobs" ]
