# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tw-erp-punch-fastify`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tw-erp-punch-fastify`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tw-erp-punch-fastify && \
  adduser --system -G tw-erp-punch-fastify tw-erp-punch-fastify

COPY dist/tw/erp/punch-fastify tw-erp-punch-fastify
RUN chown -R tw-erp-punch-fastify:tw-erp-punch-fastify .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tw-erp-punch-fastify --omit=dev -f install

CMD [ "node", "tw-erp-punch-fastify" ]
