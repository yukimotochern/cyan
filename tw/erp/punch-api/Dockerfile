# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tw-erp-punch-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tw-erp-punch-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tw-erp-punch-api && \
          adduser --system -G tw-erp-punch-api tw-erp-punch-api

COPY dist/tw/erp/punch-api tw-erp-punch-api
RUN chown -R tw-erp-punch-api:tw-erp-punch-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tw-erp-punch-api --omit=dev -f install

CMD [ "node", "tw-erp-punch-api" ]
